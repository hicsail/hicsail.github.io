import { Box } from '@chakra-ui/react';
import * as React from 'react';
import { Layout } from '../components/Layout';

export const Publications: React.VFC = () => (
  <Layout title="Publications"></Layout>
);
